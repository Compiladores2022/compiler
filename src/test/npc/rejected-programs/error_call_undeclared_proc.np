program {
    
    integer sum(integer x, integer y) {
        return x+y;
    }

    void main() {
        bool p = true;
        integer w = 3;
        integer z = suma(w, p);
        return;
    }

}
