program {
    
    integer sum(integer x, integer y) {
        return x+y;
    }

    void main() {
        integer w = 4;
        integer v = 5;
        bool q = false;
        integer z = sum(w);
        return;
    }

}
