program {

    bool p = false;
    bool q = true;

    void print(integer x) extern;

    integer main() {
        if (!(!p || q) == false) then {
            print(0);
            return 0;
        } else {
            print(1);
            return 1;
        }
        print(0);
        return 0;
    }

}
