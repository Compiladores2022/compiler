program {

    bool p = false;
    bool q = true;

    integer main() {
        if (!(!p || q) == false) then {
            return 0;
        } else {
            return 1;
        }
        return 0;
    }

}
