program {

    integer y = 5;
    
    bool main () {
        return  (false && true || true);
        /* & tiene predencia sobre el | - resultado esperado = true */
        // int x = 3;
    }

}

// return x - y;
