program {

    integer  aux = 32;
    bool b1 = (false || true);
    integer  x1 =2;
    bool b2 = false && true;

    void print(integer x) extern;

    integer main() {
        aux = (33);
        print(aux);
        return aux;
    }

}
