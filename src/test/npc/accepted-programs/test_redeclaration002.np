program {

    integer x = 12;
    integer y = 3;
    bool z = true;

    integer f(integer y) {
    	integer x = 1;
        if (x > 1) then {
            bool z = false;
            z = z && (x > 1);
        }
        return x % y;
}

    void main() {
       f(2);
    }
}
