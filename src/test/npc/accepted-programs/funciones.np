program{

bool x = false;
integer y = 0;
bool z = true;

void print(integer x) extern;

bool foo_0(){
    bool a = true;
    bool b = false;
  
   
    return !(!(a == !b)); // true
}

bool foo_1(integer x){
    z = x < 5;
    return true;
}


integer foo_2(integer x, integer y){
    return 2 * x + 3 * y;
}

integer main(){
    integer b=1;
    integer c=20;    

    y = foo_2(b, c) + 20;
    foo_1(b);
    print(y);
    return y;
}
}
