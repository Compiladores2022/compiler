program {

    integer main() {
        if (false && true || true) {
           return 0;
        } else {
           return 1;
        }
        return 0;
    }

} 

// & tiene predencia sobre el | - resultado esperado = true
