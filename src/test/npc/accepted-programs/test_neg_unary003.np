program {

    bool x = true;
    bool y = false;
    bool z = !(x || y);
    bool w = !x && !y;

    void print(integer x) extern;
    
    integer main() {
        if (z == w) then {
            print(0);
            return 0;
        } else {
            print(1);
            return 1;
        }
        print(0);
        return 0;
    }

}
