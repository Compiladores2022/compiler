program {

    bool x = true;
    bool y = false;
    bool z = !(x || y);
    bool w = !x && !y;

    integer main() {
        if (z == w) {
            return 0;
        } else {
          return 1;
        }
        return 0;
    }

}
